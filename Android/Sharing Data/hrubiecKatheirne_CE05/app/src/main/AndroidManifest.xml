<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.katie.hrubieckatheirne_ce05">

    <uses-permission android:name="com.fullsail.ce05.provider.AccessData" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <permission android:name="com.fullsail.ce05.student.provider.AccessData" android:protectionLevel="dangerous"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".ArticleContentProvider"
            android:authorities="com.fullsail.ce05.student.provider"
            android:enabled="true"
            android:exported="true"
            android:permission="com.fullsail.ce05.student.provider.AccessData" />
    </application>

</manifest>